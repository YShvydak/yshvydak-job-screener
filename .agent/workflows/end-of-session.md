---
description: **End of Session: Context Compression & Sync**
---

**End of Session: Context Compression & Sync**
I am finishing this session. Prepare the project for a fresh, token-efficient "vibe coding" start.
**Execute the following optimized updates:**

1.  **Codebase Audit**: Quickly identify _only_ what changed in this session (architecture, file paths, new patterns).
2.  **Optimize [CLAUDE.md] (The Boot File)**:
    - **Goal**: Keep this file high-density but low-token.
    - **Update**: Reflect the current architecture and critical rules.
    - **Prune**: ruthlessly REMOVE obsolete "Quick Fixes", resolved issues, or redundant instructions.
    - _This file must remain a lightweight map, not a heavy archive._
3.  **Offload Details to `docs/`**:
    - If a change is complex, summarize it in [CLAUDE.md] but put the full detail in `docs/ai/` or `docs/features/`.
    - Update `docs/ai/FILE_LOCATIONS.md` only if paths changed.
4.  **Ignore**: Do not touch `TODO.md`.
5.  **Final Output**: Just confirm which files were updated.
    _Objective: Next session loads minimal context but has zero hallucinations about the project state._
